The reviewer correctly points out I need to implement Tasks 1 through 8. I will use set_plan to write the new plan carefully following constraints.

Wait, the user instruction explicitly says:
"So: no, this doesn't reflect any fixes. It looks like an earlier or partial commit from the same session rather than a corrected one — it's actually a step backward in artifact completeness, and none of the 8 substantive gaps from my last review were touched:
- Zero recall — still zero
- CERT dataset — still never fetched (empty DataFrame every time)
- No GNN/Transformer baselines
- No significance testing
- No ablation study
- No inference-time/complexity analysis
- Adversarial test — still single evasion scenario with the same train/test leakage bug
- Label fabrication still present in phase3_modeling.py
If Jules is about to attempt the actual branch submission via submit_script.py, I'd hold off — that would publish a branch whose results tables report zero malicious recall and a fabricated 10-sample "malicious" class, which won't hold up to any real review. Worth sending Jules back with the same gap list again, explicitly telling it not to submit until recall, the CERT/second-dataset requirement, and the leakage bug are actually resolved."

I MUST DO:
1. Fix zero recall. (Implement proper class balancing in models, adjust thresholds to get actual positive recall).
2. Fetch CERT dataset properly. (Download it from Kaggle API or a publicly accessible URL instead of the HTML page. For Kaggle, use kagglehub or similar, or find a mirror).
3. Implement GNN / Transformer baselines (Task 1-8).
4. Implement Significance testing.
5. Implement Ablation study.
6. Implement Inference-time / complexity analysis.
7. Fix Adversarial test to include multiple evasion scenarios and fix train/test leakage bug.
8. Remove Label fabrication in `phase3_modeling.py`.

Let's modify the plan to address all of these.
