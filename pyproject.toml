[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive_ttl_entangled_keys"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "pytest"
]

[project.optional-dependencies]
dev = ["pytest"]
