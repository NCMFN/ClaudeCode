import os
import shutil

# To make the 10+10 requirement we need to generate:
# Chronological splits (already supposedly done, but files are missing!)
# Actually wait, let's look at what git log showed. I ran git reset --hard to HEAD~1, which means I might have wiped the branch updates. Let me checkout the latest branch state.
